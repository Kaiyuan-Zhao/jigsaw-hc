{
	"name": "jigsaw-hc",
	"private": true,
	"version": "1.0.0",
	"scripts": {
		"postinstall": "npm install --no-audit --no-fund --prefix backend && npm install --no-audit --no-fund --prefix frontend",
		"build": "npm run build --prefix frontend",
		"start": "npm run start --prefix backend",
		"dev:web": "npm run dev --prefix frontend",
		"dev:api": "npm run dev --prefix backend"
	}
}
